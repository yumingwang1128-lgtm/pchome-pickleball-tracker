# PChome 匹克球拍價格追蹤 — 進度紀錄

## 專案資訊

| 欄位 | 內容 |
| --- | --- |
| 專案名稱 | PChome 商品資料自動化爬取與價格時序資料庫建置 |
| MVP 主題 | 匹克球拍（新興運動品項） |
| 平台 | PChome 商品搜尋／分類頁；實際取得方式須先完成合規確認 |
| 核心技術 | Python、SQLite、pandas、GitHub Actions、Streamlit |
| 目前狀態 | 本機核心實作完成並通過測試；排程、部署與搜尋量驗證尚未完成 |
| 最後更新 | 2026-09-10 |

## 已確認的決策

| 日期 | 決策 | 理由／影響 |
| --- | --- | --- |
| 2026-09-03 | MVP 只追蹤匹克球拍 | 讓商品可比性高、欄位一致，能在短期內完成端到端管線。 |
| 2026-09-03 | 使用三個搜尋別名 | `匹克球拍`、`皮克球拍`、`pickleball paddle`，並以商品 ID 或正規化 URL 去重。 |
| 2026-09-03 | SQLite 作為歷史資料庫 | 單檔、易攜帶，適合教學專案與資料交付。 |
| 2026-09-03 | 搜尋量研究與價格爬蟲分開 | PChome 商品資料只能反映商品與價格，不能直接證明 Google 搜尋量變化。 |
| 2026-09-10 | 即時來源必須顯式啟用 | CLI 預設拒絕網路請求；使用者須加上 `--live`，且來源會以至少兩秒間隔讀取公開頁面。 |
| 2026-09-10 | 品牌、評分、評價數設為可空 | 兩個實測商品頁未回傳結構化品牌；無評論商品也未回傳評分或評價數。 |
| 2026-09-10 | 完成受限公開頁實測 | 修正 Next.js 分類資料的名稱關聯與配件排除後，新的本機資料庫已寫入 5 項匹克球拍快照；舊的含配件測試資料已移至 `data/tracker.pre-filter-test-20260910.db`。# PChome 匹克球拍價格追蹤 — 工作清單

## 專案範圍與完成定義

- [ ] **確認資料取得合規性**：在開始擷取前，檢視 PChome 適用的服務條款、`robots.txt` 與 Firecrawl 服務條款；將允許範圍、頻率及識別方式記錄在 `README.md`。
- [ ] **鎖定 MVP 範圍**：只追蹤 PChome 的「匹克球拍／皮克球拍」商品；搜尋別名為 `匹克球拍`、`皮克球拍`、`pickleball paddle`。
- [ ] **設定完成門檻**：每日能成功收集商品快照；資料庫可保留歷史；儀表板可呈現價格、品牌與新品趨勢；部署網址可公開瀏覽。

## 1. 專案基礎與設定

- [x] 建立 Python 專案結構：`src/`、`tests/`、`data/`、`.github/workflows/` 與 `app.py`。
- [x] 建立依賴與設定檔：固定 Python 版本，加入 `pandas`、`streamlit`、HTTP／HTML 解析套件與測試工具；機密值只由環境變數或 GitHub Secrets 讀取。
- [x] 撰寫 `README.md`：說明資料來源、執行方式、環境變數、資料限制與合規注意事項。
- [ ] 驗收：全新環境可依 README 完成安裝，並能執行空的測試套件。

## 2. 商品擷取與正規化

- [x] 定義商品快照資料契約：商品 ID、名稱、品牌、目前售價、原價、折扣率、評分、評價數、商品 URL、搜尋詞、擷取時間與原始頁面識別資訊。
- [ ] 實作欄位清理：價格轉整數、品牌缺值保留為 `NULL`、評分／評價數轉數值、URL 正規化。
- [ ] 實作搜尋別名合併與商品去重規則；優先使用穩定商品 ID，無 ID 時以正規化 URL 作退回鍵。
- [ ] 為成功解析、缺少欄位、格式異常與去重各寫測試。
- [ ] 驗收：每個別名都能輸出符合資料契約的快照，且重複商品只留下單筆。

## 3. SQLite 歷史資料庫

- [ ] 設計並建立資料表：`products`（商品主檔）、`price_snapshots`（時間序列快照）、`crawl_runs`（每次執行結果）。
- [ ] 為商品鍵、擷取時間與搜尋詞建立索引；以唯一鍵避免同一次執行寫入重複快照。
- [ ] 實作資料庫初始化、upsert 商品主檔、插入快照與記錄執行狀態。
- [x] 為資料庫約束、重複寫入與交易失敗復原寫測試。
- [ ] 驗收：連續兩次寫入相同商品時，商品主檔不重複，歷史快照可依日期查詢。

## 4. 批次管線與資料品質

- [ ] 建立批次執行器，依序處理三個搜尋別名並彙整結果。
- [ ] 加入可設定的逾時、有限重試、退避等待與失敗紀錄；不可無限重試。
- [ ] 建立品質檢查：必要欄位、非負價格、原價不得低於售價（若兩者皆存在）、商品數異常波動、重複率。
- [ ] 將品質結果與執行統計寫入 `crawl_runs`，並在異常時以非零結束碼結束。
- [ ] 驗收：模擬網路失敗、空結果與異常價格時，管線留下可診斷紀錄且不寫入不合法快照。

## 5. 排程與維運

- [ ] 建立 GitHub Actions workflow：每日定時執行，並支援手動觸發。
- [ ] 設定資料庫保存方式；確認 GitHub Actions 的執行環境與 Streamlit 部署環境能讀到同一份更新後資料。
- [ ] 將 Firecrawl 或其他服務憑證放入 GitHub Secrets，不寫入程式碼、資料庫或日誌。
- [ ] 在 workflow 中執行測試與品質檢查；失敗時保留足以除錯的非敏感日誌。
- [ ] 驗收：手動觸發一次成功，下一次執行能累積新快照而非覆蓋歷史。

## 6. Streamlit 儀表板

- [ ] 建立資料讀取層，提供日期區間、品牌與價格帶篩選。
- [ ] 製作 KPI：追蹤商品數、品牌數、最新中位數價格、期間內新上架商品數。
- [ ] 製作圖表：每日平均／中位數價格、價格帶分布、品牌商品數、降價幅度排行、新上架趨勢。
- [ ] 清楚標示資料更新時間、樣本數、篩選條件，以及「價格變動不代表成交價」的限制。
- [ ] 驗收：空資料、單日資料及多日資料均可正常呈現，且篩選結果與資料庫查詢一致。

## 7. 部署、展示與結案

- [ ] 部署至 Streamlit Community Cloud，設定必要環境變數與資料庫讀取方式。
- [ ] 進行公開展示驗收：首頁載入、圖表篩選、行動版基本可讀性與錯誤頁面。
- [ ] 撰寫結案報告：專案目標、架構、資料欄位、品質策略、分析結果、限制與下一步。
- [ ] 準備展示案例：挑選一個品牌與一段降價趨勢，說明資料如何轉成商業洞察。

## 8. 六個月搜尋量驗證（獨立研究工作）

- [ ] 取得可重現的台灣搜尋量來源，例如 Google Trends 匯出資料或具授權的關鍵字工具。
- [ ] 定義「顯著上升」：比較最近 8 週與前 8 週的平均指數／搜尋量，設定上升門檻，並排除單一賽事或新聞造成的尖峰。
- [ ] 比較 `匹克球`、`匹克球拍`、`皮克球`、`皮克球拍` 及至少兩個對照品類。
- [ ] 將來源、時間範圍、地區、方法與結果放入結案報告；未取得原始時間序列前，不宣稱已證實「近六個月顯著上升」。

## 建議執行順序

1. 範圍／合規 → 基礎設定 → 單次擷取。
2. 資料庫 → 批次管線 → 品質檢查。
3. 先累積連續資料，再完成排程、儀表板與部署。
4. 搜尋量驗證可平行進行，但不阻塞價格追蹤 MVP。
 |
# PChome 匹克球拍價格追蹤 — 工作清單

## 專案範圍與完成定義

- [ ] **確認資料取得合規性**：在開始擷取前，檢視 PChome 適用的服務條款、`robots.txt` 與 Firecrawl 服務條款；將允許範圍、頻率及識別方式記錄在 `README.md`。
- [ ] **鎖定 MVP 範圍**：只追蹤 PChome 的「匹克球拍／皮克球拍」商品；搜尋別名為 `匹克球拍`、`皮克球拍`、`pickleball paddle`。
- [ ] **設定完成門檻**：每日能成功收集商品快照；資料庫可保留歷史；儀表板可呈現價格、品牌與新品趨勢；部署網址可公開瀏覽。

## 1. 專案基礎與設定

- [x] 建立 Python 專案結構：`src/`、`tests/`、`data/`、`.github/workflows/` 與 `app.py`。
- [x] 建立依賴與設定檔：固定 Python 版本，加入 `pandas`、`streamlit`、HTTP／HTML 解析套件與測試工具；機密值只由環境變數或 GitHub Secrets 讀取。
- [x] 撰寫 `README.md`：說明資料來源、執行方式、環境變數、資料限制與合規注意事項。
- [ ] 驗收：全新環境可依 README 完成安裝，並能執行空的測試套件。

## 2. 商品擷取與正規化

- [x] 定義商品快照資料契約：商品 ID、名稱、品牌、目前售價、原價、折扣率、評分、評價數、商品 URL、搜尋詞、擷取時間與原始頁面識別資訊。
- [ ] 實作欄位清理：價格轉整數、品牌缺值保留為 `NULL`、評分／評價數轉數值、URL 正規化。
- [ ] 實作搜尋別名合併與商品去重規則；優先使用穩定商品 ID，無 ID 時以正規化 URL 作退回鍵。
- [ ] 為成功解析、缺少欄位、格式異常與去重各寫測試。
- [ ] 驗收：每個別名都能輸出符合資料契約的快照，且重複商品只留下單筆。

## 3. SQLite 歷史資料庫

- [ ] 設計並建立資料表：`products`（商品主檔）、`price_snapshots`（時間序列快照）、`crawl_runs`（每次執行結果）。
- [ ] 為商品鍵、擷取時間與搜尋詞建立索引；以唯一鍵避免同一次執行寫入重複快照。
- [ ] 實作資料庫初始化、upsert 商品主檔、插入快照與記錄執行狀態。
- [x] 為資料庫約束、重複寫入與交易失敗復原寫測試。
- [ ] 驗收：連續兩次寫入相同商品時，商品主檔不重複，歷史快照可依日期查詢。

## 4. 批次管線與資料品質

- [ ] 建立批次執行器，依序處理三個搜尋別名並彙整結果。
- [ ] 加入可設定的逾時、有限重試、退避等待與失敗紀錄；不可無限重試。
- [ ] 建立品質檢查：必要欄位、非負價格、原價不得低於售價（若兩者皆存在）、商品數異常波動、重複率。
- [ ] 將品質結果與執行統計寫入 `crawl_runs`，並在異常時以非零結束碼結束。
- [ ] 驗收：模擬網路失敗、空結果與異常價格時，管線留下可診斷紀錄且不寫入不合法快照。

## 5. 排程與維運

- [ ] 建立 GitHub Actions workflow：每日定時執行，並支援手動觸發。
- [ ] 設定資料庫保存方式；確認 GitHub Actions 的執行環境與 Streamlit 部署環境能讀到同一份更新後資料。
- [ ] 將 Firecrawl 或其他服務憑證放入 GitHub Secrets，不寫入程式碼、資料庫或日誌。
- [ ] 在 workflow 中執行測試與品質檢查；失敗時保留足以除錯的非敏感日誌。
- [ ] 驗收：手動觸發一次成功，下一次執行能累積新快照而非覆蓋歷史。

## 6. Streamlit 儀表板

- [ ] 建立資料讀取層，提供日期區間、品牌與價格帶篩選。
- [ ] 製作 KPI：追蹤商品數、品牌數、最新中位數價格、期間內新上架商品數。
- [ ] 製作圖表：每日平均／中位數價格、價格帶分布、品牌商品數、降價幅度排行、新上架趨勢。
- [ ] 清楚標示資料更新時間、樣本數、篩選條件，以及「價格變動不代表成交價」的限制。
- [ ] 驗收：空資料、單日資料及多日資料均可正常呈現，且篩選結果與資料庫查詢一致。

## 7. 部署、展示與結案

- [ ] 部署至 Streamlit Community Cloud，設定必要環境變數與資料庫讀取方式。
- [ ] 進行公開展示驗收：首頁載入、圖表篩選、行動版基本可讀性與錯誤頁面。
- [ ] 撰寫結案報告：專案目標、架構、資料欄位、品質策略、分析結果、限制與下一步。
- [ ] 準備展示案例：挑選一個品牌與一段降價趨勢，說明資料如何轉成商業洞察。

## 8. 六個月搜尋量驗證（獨立研究工作）

- [ ] 取得可重現的台灣搜尋量來源，例如 Google Trends 匯出資料或具授權的關鍵字工具。
- [ ] 定義「顯著上升」：比較最近 8 週與前 8 週的平均指數／搜尋量，設定上升門檻，並排除單一賽事或新聞造成的尖峰。
- [ ] 比較 `匹克球`、`匹克球拍`、`皮克球`、`皮克球拍` 及至少兩個對照品類。
- [ ] 將來源、時間範圍、地區、方法與結果放入結案報告；未取得原始時間序列前，不宣稱已證實「近六個月顯著上升」。

## 建議執行順序

1. 範圍／合規 → 基礎設定 → 單次擷取。
2. 資料庫 → 批次管線 → 品質檢查。
3. 先累積連續資料，再完成排程、儀表板與部署。
4. 搜尋量驗證可平行進行，但不阻塞價格追蹤 MVP。

## 市場驗證紀錄

- 匹克球是本案的「新興運動」候選；PChome 已有匹克球／拍分類，適合建立可比較的商品資料集。
- 飛比價格 2026 年運動用品排行將匹克球拍列入前段熱門商品，但其為站內搜尋與互動熱度，不能直接等同 Google 搜尋量。
- 台灣近六個月「搜尋量顯著上升」尚待取得可重現的逐週／逐月原始資料後驗證；目前不列為已證實結論。

## 里程碑

| 里程碑 | 目標 | 狀態 | 完成條件 |
| --- | --- | --- | --- |
| M0：範圍與合規 | 確認資料來源與 MVP | 進行中 | 條款／頻率／資料欄位已記錄，且主題固定為匹克球拍。 |
| M1：單次擷取 | 取得可用商品快照 | 進行中 | 已驗證 5 項匹克球拍的實際快照可寫入 SQLite；仍需取得三個別名與後續新增商品。 |
| M2：歷史資料庫 | 可保留每日資料 | 完成 | SQLite upsert、歷史快照與 Windows 連線關閉已由單元測試驗證。 |
| M3：自動化與品質 | 每日可靠執行 | 進行中 | 資料品質檢查與手動 Actions workflow 已完成；每日排程尚未啟用。 |
| M4：儀表板與部署 | 可公開展示洞察 | 進行中 | 儀表板程式已建立；專案環境的 Streamlit 相依套件尚未完成安裝，未進行啟動測試。 |
| M5：搜尋量驗證 | 驗證六個月趨勢 | 未開始 | 有保存的原始時間序列與明確比較方法。 |

## 目前阻塞與風險

| 項目 | 影響 | 因應方式 | 狀態 |
| --- | --- | --- | --- |
| 資料取得規則尚未確認 | 可能無法以原規劃方式擷取 | 先檢查 PChome 與所選服務的條款、robots 與使用限制；必要時改用授權或公開介面。 | 待處理 |
| Firecrawl 成本／金鑰 | 排程可能失敗或超出免費額度 | 將使用量、重試與密鑰管理納入 workflow 設計。 | 待處理 |
| 搜尋量資料尚未取得 | 不能支撐「六個月顯著上升」主張 | 另行取得可重現的台灣搜尋量時間序列，採固定門檻判定。 | 待處理 |
| Streamlit 套件尚未安裝完成 | 無法在本機啟動並驗證儀表板 | 使用專案 `.venv` 完成 `pip install -r requirements.txt` 後，再啟動 Streamlit。 | 待處理 |
| 商品名稱與別名重複 | 會扭曲商品數和價格統計 | 以商品 ID／正規化 URL 去重，保留來源別名供追查。 | 待處理 |
| 專案原時程已進入後段 | 可能壓縮資料累積天數 | 優先完成單一品類與每日快照，將擴充品類列為後續版本。 | 待處理 |

## 下一步

1. 完成資料取得合規檢查，決定可用的擷取方式與頻率。
2. 建立最小專案骨架與商品快照資料契約。
3. 完成一個搜尋別名的單次擷取與 SQLite 寫入，再擴充到其餘別名。

## 更新規則

- 每完成一個 TODO 項目，更新對應里程碑狀態與完成條件。
- 每次排程執行後，記錄成功／失敗、商品數、去重數、異常數與資料庫快照數。
- 所有市場結論必須附資料來源與觀察區間；無時間序列資料時，使用「市場熱度候選」而非「搜尋量顯著上升」。
